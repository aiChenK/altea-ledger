# Node dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build outputs
dist/
dist-ssr/
build/

# IDEs and Editors
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.DS_Store

# Local application data
# Ignore personal character progress and history snapshots
data/data.json
data/history.json

# Note: data/config.json is kept untracked by git only if you want it entirely local.
# We keep it tracked here to provide default roles and weeklies configuration templates on first load.
